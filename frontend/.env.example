# API base URL (include /api suffix)
VITE_API_URL=http://localhost:5000/api

# Temporary: open /admin without login (must also set ADMIN_AUTH_DISABLED=true on the API)
VITE_ADMIN_AUTH_DISABLED=true

# Optional: WhatsApp number with country code, no + or spaces (e.g. 919876543210)
VITE_WHATSAPP_NUMBER=
